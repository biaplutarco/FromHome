# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'FromHome' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FromHome
  pod 'SwiftLint'

  target 'FromHomeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FromHomeUITests' do
    # Pods for testing
  end

end
